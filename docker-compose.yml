version: "3.8"

services:
  # mysql:
  #   image: mysql:8.0
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root
  #     MYSQL_DATABASE: media_tracker
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  app:
    build: .
    ports:
      - "8084:8080"
    environment:
      - DATABASE_HOST=192.168.2.148
      - DATABASE_PORT=3306
      - DATABASE_USER=root
      - DATABASE_PASSWORD=jdmliyk1
      - DATABASE_DBNAME=media_tracker
